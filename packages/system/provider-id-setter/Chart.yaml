apiVersion: v2
name: cozy-provider-id-setter
description: DaemonSet that sets spec.providerID on Kubernetes nodes running on OpenStack (Switch Cloud) via IMDS.
type: application
version: 0.0.0 # Placeholder, the actual version will be automatically set during the build process
appVersion: "0.4.2"
