apiVersion: v2
name: cozy-kilo-clustermesh-operator
version: 0.0.0 # Placeholder, the actual version will be automatically set during the build process
description: ClusterMesh operator for Kilo — manages cross-cluster WireGuard Peer objects across tenant, workload and Ceph clusters
