apiVersion: v2
name: cozy-capi-bootstrap-talos
description: Cluster API Bootstrap Provider Talos (CABPT) — system package for kubernetes-switchcloud
type: application
version: 0.0.0 # Placeholder, the actual version will be automatically set during the build process
appVersion: "0.6.8"
keywords:
  - cluster-api
  - talos
  - bootstrap
