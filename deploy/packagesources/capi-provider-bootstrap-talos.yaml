---
apiVersion: cozystack.io/v1alpha1
kind: PackageSource
metadata:
  name: cozystack.capi-provider-bootstrap-talos
spec:
  sourceRef:
    kind: OCIRepository
    name: kubernetes-switchcloud-packages
    namespace: cozy-system
    path: /
  variants:
  - name: default
    dependsOn:
    - cozystack.networking
    - cozystack.capi-operator
    components:
    - name: capi-providers-bootstrap-talos
      path: system/capi-providers-bootstrap-talos
      install:
        privileged: true
        namespace: cozy-cluster-api
        releaseName: capi-providers-bootstrap-talos
