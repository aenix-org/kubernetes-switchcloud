image:
  repository: ghcr.io/aenix-org/talos-trustd-router
  tag: ""  # defaults to .Chart.AppVersion
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    memory: 64Mi
