apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: InfrastructureProvider
metadata:
  name: openstack
spec:
  version: v0.12.1-infraprovider-openstack
  fetchConfig:
    selector:
      matchLabels:
        infraprovider-components: openstack
