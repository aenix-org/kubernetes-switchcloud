apiVersion: v1
kind: Service
metadata:
  name: talos-edge-router
spec:
  selector:
    app: talos-edge-router
  ports:
  {{- range .Values.listeners }}
  - name: {{ .name }}
    port: {{ .port }}
    targetPort: {{ .name }}
    protocol: TCP
  {{- end }}
